"""Tests for the product package.

Every test here is offline. The suite must be runnable by a judge, on a laptop
with no API key, without spending a credit — so the network transport is always a
stub and the cache is always a temp directory built by the test itself.
"""
